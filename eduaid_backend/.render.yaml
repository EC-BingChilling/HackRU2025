services:
  - type: web
    name: eduaid-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    rootDir: eduaid_backend
    pythonVersion: 3.12.3
